{
    "require-dev": {
        "phpunit/phpunit": "10.0.*"
    },
    "scripts" :{
        "make_report": [
            "XDEBUG_MODE=coverage /var/www/core/vendor/bin/phpunit --coverage-html tests"
        ],
        "test": [
            "/var/www/core/vendor/bin/phpunit tests"
        ]
    }
}